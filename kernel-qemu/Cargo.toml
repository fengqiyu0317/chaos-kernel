[package]
name = "kernel-qemu"
version = "0.1.0"
edition = "2021"

[features]
qemu-mm-selftest = []
qemu-sched-selftest = []
qemu-sync-selftest = []
qemu-selftest = ["qemu-mm-selftest", "qemu-sched-selftest", "qemu-sync-selftest"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
